import { FullWidthContainer } from '../../components/FullWidthContainer'
import { LotsList } from './components';

const LotsPage = () => {
  return (
    <FullWidthContainer>
      <LotsList />
    </FullWidthContainer>
  );
};

export default LotsPage;
